/**
 * Startie's face. Bump the filename version whenever the artwork changes so
 * browsers and the Vercel image CDN refetch instead of serving the old file
 * (next/image rejects query strings without an images.localPatterns entry).
 */
export const STARTIE_ICON_SRC = "/startie-v2.png";
