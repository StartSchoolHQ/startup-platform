/**
 * Startie's face. Bump the version whenever `public/startie.png` changes so
 * browsers and the Vercel image CDN refetch instead of serving the old file.
 */
export const STARTIE_ICON_SRC = "/startie.png?v=2";
